//// Use this contract to store the script reference UTxOs for the wallet and 
//// seedelf contract so they have to exist forever.
////

validator contract(_random: ByteArray) {
  //
  // (\(\
  // ( -.-)
  // o_(")(")
  //
  else(_) {
    fail @"I always fail :("
  }
}
