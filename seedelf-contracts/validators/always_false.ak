//// Use this contract to store script reference UTxOs so they exist forever.
////

validator contract(_any: Data) {
  //
  // (\(\    
  // ( -.-)   
  // o_(")(")
  else(_) {
    fail @"I always fail :/"
  }
}
