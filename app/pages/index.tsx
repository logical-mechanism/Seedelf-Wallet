import type { NextPage } from "next";
import NavBar from "@/components/nav-bar";

const Home: NextPage = () => {

  return (
    <div className="flex">
      <NavBar/>
    </div>
  );
};

export default Home;
