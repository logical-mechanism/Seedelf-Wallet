# The Seedelf Desktop Application

Current goal: Build an electron app using oura + flask for the back and nextjs + react on the front.

```bash
python3 -m venv venv
source venv/bin/activate
pip install requirements.txt
```